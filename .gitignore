node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.*
!.env.example

# editor
.vscode/*
!.vscode/extensions.json
.idea

# vite / pwa
dev-dist
*.tsbuildinfo

# capacitor native projects (generated with `npx cap add`)
/android
/ios

# generated promo video and its assets — megabytes of media the app never ships
/brag-output

# Claude Code local state (worktrees, session scratch) — not project content
/.claude
