import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.bacilek.efecto',
  appName: 'Efecto',
  webDir: 'dist',
  backgroundColor: '#12141c',
  android: {
    backgroundColor: '#12141c',
  },
}

export default config
